{
    "id": "warmly",
    "name": "warmly",
    "description": "warmly agent setup.",
    "required_env": ["CLIENT_ID", "STACK_NAME", "WAHA_DASHBOARD_PASSWORD", "WAHA_API_KEY_PLAIN", "WAHA_API_KEY"],
    "optional_env": ["BIGQUERY_TABLE_ID"],
    "config": {
        "networks": ["edge", "internal", "shared"],
        "traefik_enabled": true,
        "requires_database": true
    }
}
